pipeline {
    agent any
    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    docker.build('my-selenium-test')
                }
            }
        }
        stage('Run SonarQube Scanner') {
            steps {
                script {
                    docker.image('my-selenium-test').inside('-w /app -v /c/ProgramData/Jenkins/.jenkins/workspace/testY:/app') {
                        // Replace this with your SonarQube commands
                        sh 'mvn sonar:sonar'
                    }
                }
            }
        }
    }
    post {
        failure {
            script {
                emailext(
                    subject: "Jenkins Build Failed: ${currentBuild.fullDisplayName}",
                    body: "Build failed. Please check the Jenkins console output for more details.",
                    to: "saharheni6@gmail.com, sahar.heni@ensi-uma.tn"
                )
            }
        }
    }
}
